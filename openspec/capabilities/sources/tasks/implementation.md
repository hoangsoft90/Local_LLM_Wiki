# Tasks — Sources

> Implemented — see `agent_wiki/lib/data/wiki_repository.dart` (`importSource`).

- [x] Import file via picker → record source + SHA-256 hash.
- [x] Versioning on content change (version++), no-op on identical hash.
- [x] Write `sources/<id>.md` canonical (+ history on version bump).
- [x] TEST-001 coverage.
