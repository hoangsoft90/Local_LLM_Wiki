# Tasks — Promote & Inbox

> Implemented — see `agent_wiki/lib/domain/promote_service.dart` + `ui/inbox_screen.dart`.

- [x] Draft bundle persistence (`inbox/draft_<id>.json`).
- [x] Accept → apply via patch engine; Reject → discard with reason.
- [x] Cross-model corroboration triggers (status upgrade / ≥2-source merge) + force-accept override.
- [x] Inbox UI with Accept/Reject + corroboration note.
- [x] TEST-004, TEST-010 coverage.
