# Tasks — Claims & Evidence

> Implemented — see `agent_wiki/lib/core/models/models.dart` + `ui/widgets/claim_card.dart`.

- [x] Claim model + JSON serialization with full contract fields.
- [x] Evidence list with (source_id, source_version, location, quote).
- [x] Status transition validation (agent blocked from cross_checked/human_verified).
- [x] Claim cards UI with hierarchy labels/colors + staleness warning.
